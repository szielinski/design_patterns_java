Purpose: Attach additional responsibilities to an object dynamically.

Use composition with a single-parent class hierarchy to define a family of decorating classes that can add functionality at runtime before or after the original method calls.
