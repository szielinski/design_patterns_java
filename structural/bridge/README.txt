Purpose: decouple an abstraction from its implemenation so that the two can vary independently

Establish two class hierarchies that can grow and evolve independently reducing the overall number of classes required for implementation
